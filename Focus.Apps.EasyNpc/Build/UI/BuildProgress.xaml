<UserControl x:Class="Focus.Apps.EasyNpc.Build.UI.BuildProgress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:bp="clr-namespace:Focus.Apps.EasyNpc.Build.Pipeline"
             xmlns:bu="clr-namespace:Focus.Apps.EasyNpc.Build.UI"
             xmlns:fa="http://schemas.fontawesome.com/icons/"
             xmlns:local="clr-namespace:Focus.Apps.EasyNpc"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=bu:BuildProgressViewModel`1}"
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded">
    <ui:ScrollViewerEx>
        <ui:SimpleStackPanel>
            <!-- Build metadata: overall progress, elapsed time, task count and estimated time remaining. -->
            <Border Background="{DynamicResource SystemControlBackgroundListLowBrush}"
                    BorderBrush="{DynamicResource SystemControlHighlightAccentBrush}"
                    BorderThickness="0,0,0,2"
                    CornerRadius="6,6,0,0"
                    Margin="0,0,8,12"
                    Padding="14,10">
                <ui:SimpleStackPanel Spacing="8">
                    <Grid>
                        <ui:SimpleStackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <!-- Two cogs, each spinning on its own centre in opposite directions and at different speeds. -->
                            <Grid Width="18" Height="16" Margin="0,0,6,0"
                                  Visibility="{Binding IsBuildComplete, Converter={StaticResource InvBoolToVisibility}}">
                                <Grid.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="CogBig" Storyboard.TargetProperty="Angle"
                                                                 From="0" To="360" Duration="0:0:2.6" RepeatBehavior="Forever"/>
                                                <DoubleAnimation Storyboard.TargetName="CogSmall" Storyboard.TargetProperty="Angle"
                                                                 From="360" To="0" Duration="0:0:1.7" RepeatBehavior="Forever"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Grid.Triggers>
                                <fa:SvgAwesome Icon="Solid_Cog" Width="11" Height="11"
                                               HorizontalAlignment="Left" VerticalAlignment="Top"
                                               Foreground="{DynamicResource SystemControlHighlightAccentBrush}"
                                               RenderTransformOrigin="0.5,0.5">
                                    <fa:SvgAwesome.RenderTransform>
                                        <RotateTransform x:Name="CogBig"/>
                                    </fa:SvgAwesome.RenderTransform>
                                </fa:SvgAwesome>
                                <fa:SvgAwesome Icon="Solid_Cog" Width="8" Height="8"
                                               HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                               Foreground="{DynamicResource SystemControlHighlightAccentBrush}"
                                               RenderTransformOrigin="0.5,0.5">
                                    <fa:SvgAwesome.RenderTransform>
                                        <RotateTransform x:Name="CogSmall"/>
                                    </fa:SvgAwesome.RenderTransform>
                                </fa:SvgAwesome>
                            </Grid>
                            <TextBlock VerticalAlignment="Center">
                                <Run FontWeight="SemiBold" Text="{Binding CompletedTaskCount, Mode=OneWay}"/><Run Text=" / "/><Run Text="{Binding TotalTaskCount, Mode=OneWay}"/><Run Text=" tasks"/>
                            </TextBlock>
                        </ui:SimpleStackPanel>
                        <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="16"
                                   Foreground="{DynamicResource SystemControlHighlightAccentBrush}">
                            <Run Text="{Binding OverallProgressPercent, Mode=OneWay}"/><Run Text="%"/>
                        </TextBlock>
                    </Grid>
                    <ui:ProgressBar Minimum="0" Maximum="100" Value="{Binding OverallProgressPercent, Mode=OneWay}"/>
                    <Grid>
                        <TextBlock Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Center">
                            <fa:SvgAwesome Icon="Regular_Clock" Height="12" Margin="0,0,5,-1"/>
                            <Run Text="Elapsed"/> <Run FontWeight="SemiBold" Text="{Binding ElapsedText, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock HorizontalAlignment="Right" Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Center"
                                   Visibility="{Binding IsBuildComplete, Converter={StaticResource InvBoolToVisibility}}">
                            <fa:SvgAwesome Icon="Regular_Hourglass" Height="12" Margin="0,0,5,-1"/>
                            <Run Text="Est. remaining"/> <Run FontWeight="SemiBold" Text="{Binding EstimatedTimeRemainingText, Mode=OneWay}"/>
                        </TextBlock>
                    </Grid>
                </ui:SimpleStackPanel>
            </Border>
            <Border Margin="0,0,-16,0">
                <DataGrid x:Name="TaskGrid"
                          AutoGenerateColumns="False"
                          BorderBrush="Transparent"
                          BorderThickness="0"
                          HeadersVisibility="None"
                          IsReadOnly="True"
                          ItemsSource="{Binding Tasks}"
                          local:ForceTextTrimming.ForceTextTrimming="CharacterEllipsis"
                          PreviewMouseWheel="TaskGrid_PreviewMouseWheel"
                          d:ItemsSource="{d:DesignInstance bu:BuildProgressSampleData, IsDesignTimeCreatable=True}">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource DataGridRowStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding State}" Value="{x:Static bp:BuildTaskState.Cancelled}">
                                    <Setter Property="Background" Value="{StaticResource TaskProgressCancelledBackgroundBrush}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding State}" Value="{x:Static bp:BuildTaskState.Completed}">
                                    <Setter Property="Background" Value="{StaticResource TaskProgressCompletedBackgroundBrush}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding State}" Value="{x:Static bp:BuildTaskState.Failed}">
                                    <Setter Property="Background" Value="{StaticResource TaskProgressFailedBackgroundBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Status" Width="32">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <fa:SvgAwesome Height="20" Width="20">
                                        <fa:SvgAwesome.Style>
                                            <Style TargetType="fa:SvgAwesome">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding State}" Value="{x:Static bp:BuildTaskState.Cancelled}">
                                                        <Setter Property="Icon" Value="Regular_StopCircle"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding State}" Value="{x:Static bp:BuildTaskState.Completed}">
                                                        <Setter Property="Icon" Value="Regular_CheckCircle"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding State}" Value="{x:Static bp:BuildTaskState.Failed}">
                                                        <Setter Property="Icon" Value="Regular_TimesCircle"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding State}" Value="{x:Static bp:BuildTaskState.NotStarted}">
                                                        <Setter Property="Icon" Value="Regular_Clock"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding State}" Value="{x:Static bp:BuildTaskState.Paused}">
                                                        <Setter Property="Icon" Value="Regular_PauseCircle"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding State}" Value="{x:Static bp:BuildTaskState.Running}">
                                                        <Setter Property="Icon" Value="Regular_PlayCircle"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </fa:SvgAwesome.Style>
                                    </fa:SvgAwesome>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Task" CellStyle="{StaticResource DataGridCellTrimmed}" Binding="{Binding Name}" Width="200"/>
                        <DataGridTextColumn Header="Item" Binding="{Binding ItemName}" Width="*"/>
                        <DataGridTemplateColumn Header="Progress" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,8,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ui:ProgressBar Grid.Column="0"
                                                        VerticalAlignment="Center"
                                                        IsIndeterminate="{Binding IsIndeterminate, Mode=OneWay}"
                                                        Minimum="{Binding MinProgress, Mode=OneWay}"
                                                        Maximum="{Binding MaxProgress, Mode=OneWay}"
                                                        Value="{Binding CurrentProgress, Mode=OneWay}"/>
                                        <!-- Live count (e.g. 44 / 814); collapses to nothing when the step isn't running. -->
                                        <TextBlock Grid.Column="1" Text="{Binding ProgressText, Mode=OneWay}"
                                                   Margin="6,0,0,0" VerticalAlignment="Center"
                                                   FontSize="11" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource MutedTextBrush}"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            <ui:SimpleStackPanel x:Name="WaitingPanel" Margin="8" Visibility="{Binding HasRemainingTasks, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Style="{StaticResource SmallHeaderStyle}" FontSize="14">Still waiting on:</TextBlock>
                <ItemsControl ItemsSource="{Binding RemainingTaskNames}">
                    <d:ItemsControl.ItemsSource>
                        <x:Array Type="{x:Type sys:String}">
                            <sys:String>Task 1</sys:String>
                            <sys:String>Task 2</sys:String>
                            <sys:String>Task 3</sys:String>
                        </x:Array>
                    </d:ItemsControl.ItemsSource>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <ItemsPresenter/>
                        </ControlTemplate>
                    </ItemsControl.Template>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:SimpleStackPanel Orientation="Horizontal" Margin="0,4,8,0">
                                <fa:SvgAwesome Icon="Regular_Clock" Height="14"/>
                                <TextBlock Text="{Binding}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                            </ui:SimpleStackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ui:SimpleStackPanel>
        </ui:SimpleStackPanel>
    </ui:ScrollViewerEx>
</UserControl>
